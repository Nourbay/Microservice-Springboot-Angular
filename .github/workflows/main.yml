name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar

      - name: Build JAR with Maven
        run: mvn clean package -DskipTests
        working-directory: ./gestion-match

      - name: List target directory
        run: ls -l ./gestion-match/target

      - name: Build and analyze
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=Nourbay_Microservice-Springboot-Angular
        working-directory: ./gestion-match
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: 🐳 Log in to Docker (Nexus)
        run: echo "${{ secrets.NEXUS_PASSWORD }}" | docker login 1870-196-234-9-161.ngrok-free.app --username ${{ secrets.NEXUS_USERNAME }} --password-stdin

      - name: Build Docker image
        run: docker build -t 1870-196-234-9-161.ngrok-free.app/repository/docker-back/gestion-match:latest .
        working-directory: ./gestion-match

      - name: Push Docker image to Nexus
        run: docker push 1870-196-234-9-161.ngrok-free.app/repository/docker-back/gestion-match:latest

        
      # Docker Hub login and push
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
        
      - name: Push images to Docker Hub
        run:
          docker push ${{ secrets.DOCKER_USERNAME }}/gestion-match:latest
